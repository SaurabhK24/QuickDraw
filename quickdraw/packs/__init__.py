"""Pack system — drop-in vertical agent configurations.

A pack is a directory containing a MANIFEST.yaml that defines agents,
their tools, workflows, and routing hints. The pack loader discovers
all packs and builds a unified registry used by the router and workflows.
"""
